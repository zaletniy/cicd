pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'ls -la'
            }
        }
        stage('Test'){
            steps {
                sh 'ls -la'
        }
        stage('Deploy') {
            steps {
                sh 'uptime'
            }
        }
    }
}
